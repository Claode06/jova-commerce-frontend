## Verification Checklist

- [x] Navbar user dropdown appears after page refresh (cookie-based)
- [x] `fetchMe()` is called in layout `onMounted`
- [x] Template uses `isLoggedIn || user` for gate
- [x] No `bg-gradient-to-b from-base-100 to-base-200` remaining
- [x] No `bg-base-100` references remain anywhere
- [x] All page backgrounds are `bg-white`
- [x] Build passes with 0 errors
